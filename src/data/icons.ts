
import logo from './logo.svg';
import siteLogo from '../../public/favicon.ico';
import { StaticImageData } from 'next/image';

export default class Icons {
  static Logo_svg : string = logo;
  static site_logo : StaticImageData = siteLogo;
}